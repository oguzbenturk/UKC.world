# Production Environment - deployed on server
# Frontend configuration for production deployment

# Application URLs (production)
BACKEND_API_URL=http://localhost:4000
VITE_BACKEND_URL=https://plannivo.com
BACKEND_API_PORT=4000

# Database Configuration (production containers)
DB_HOST=db
DB_PORT=5432
DB_NAME=plannivo
DB_USER=plannivo
DB_PASS=WHMgux86

# Security (production)
JWT_SECRET=plannivo-production-secret-key-2025

# Redis Configuration (production container)
DISABLE_REDIS=true
REDIS_HOST=redis
REDIS_PORT=6379

# Optional override for local development (leave empty in production)
LOCAL_DATABASE_URL=

# Production settings
NODE_ENV=production
LOG_LEVEL=info
ENABLE_METRICS=true

# Frontend URL (production)
FRONTEND_URL=http://plannivo.com
