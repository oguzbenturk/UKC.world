# PRODUCTION SERVER ENVIRONMENT (217.154.201.29)
# ⚠️  WARNING: This file contains SENSITIVE CREDENTIALS - NEVER commit to Git
# ⚠️  TODO: ROTATE ALL CREDENTIALS - Current values are compromised in Git history

# Database Credentials (used by Docker Compose)
POSTGRES_DB=plannivo
POSTGRES_USER=plannivo
POSTGRES_PASSWORD=WHMgux86
# TODO: Generate new password with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Database Connection String (used by application)
DATABASE_URL=postgresql://plannivo:WHMgux86@db:5432/plannivo

# JWT Authentication
JWT_SECRET=kitesurfpro-production-secret-key-2025
# TODO: Generate new secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SEC-008: Reduced from 24h to 2h for better security (shorter window for stolen tokens)
TOKEN_EXPIRY=2h

BACKEND_API_URL=http://localhost:4000
BACKEND_API_PORT=4000
NODE_ENV=production
FRONTEND_URL=http://plannivo.com

# Redis Configuration - Docker internal
REDIS_HOST=redis
REDIS_PORT=6379
# SEC-004: Redis password authentication (TODO: ROTATE with secure value)
REDIS_PASSWORD=PLACEHOLDER_CHANGE_ME_IN_PRODUCTION
# Generate secure password with: node generate-secure-credentials.js

# Cache Configuration
CACHE_DEFAULT_TTL=300
CACHE_MAX_SIZE=1000

# Performance Monitoring
ENABLE_QUERY_LOGGING=true
SLOW_QUERY_THRESHOLD=100
# Default Wallet Currency
DEFAULT_WALLET_CURRENCY=EUR

# Iyzico Payment Gateway (Sandbox for testing in production)
# TODO: Switch to production keys when ready for real payments
IYZICO_ENABLED=true
IYZICO_API_KEY=sandbox-VCCnjIPSZMaGZ6GAHYdrBlhzXaFb7WZ6
IYZICO_SECRET_KEY=sandbox-W8hOEybjBUWLfKWrnKIVYF58xzd4jzTR
IYZICO_BASE_URL=https://sandbox-api.iyzipay.com
IYZICO_TEST_MODE=true