# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local backups generated by push-all.js
.env.backup
backend/.env.backup
*.backup

# Local deployment secrets (never commit)
.deploy.secrets.json
.deploy.secrets.akyaka.json

# Diagnostic artifacts
remote-ls-output.txt

# Local temporary SSL material (never commit)
.ssl-temp/

# SSL certificates and private keys (never commit)
SSL/
SSL-akyaka/
*.key
*.crt
*.pem

# Environment files with secrets (never commit)
backend/.env
.env
backend/.env.production
.env.production
backend/.env.local
.env.local

# User-generated uploads (avatars, images, signatures — do not commit or push)
backend/uploads/**
!backend/uploads/.gitkeep

# DB export JSON dumps + copied signature images (sensitive, huge — never commit)
backend/backups/**
!backend/backups/.gitkeep

# DB sync dump (transient — cleaned up after sync, but gitignore as safety net)
prod_dump.sql

# Ad-hoc lesson marketing images (optional local only)
DuotoneFonts/Lesson pictures/

# Claude Code temporary files
tmpclaude-*
tmp-*.mjs
tmp-*.cjs

# Test output artifacts
playwright-report/
test-results/

# Last registered student for User_browser_tests (local only)
tests/e2e/User_browser_tests/.browser-student-credentials.json

# Stale production config folder
productionvars/

# Large binary assets (not used by the app)
*.pdf
pictures/

# ACME challenge webroot (local infrastructure artifact)
acme-webroot/

# Production env files with secrets
backend/.env.server.production

# Scraped product data / bulk import images
downloads-ion-duotone/
DuotoneFonts/downloads/
backend/.env.production
backend/.env.akyaka.production

# Credential backup (NEVER commit)
CREDENTIALS_BACKUP.md

# Development/test env files with secrets
backend/.env.development
.env.test
.env.production.template

# Claude Code local settings
.claude/settings.local.json
